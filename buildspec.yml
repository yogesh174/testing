version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12

  build:
    commands:
      - ./scripts/test.sh

reports:
  jest_reports:
    files:
      - junit-*.xml
    file-format: JUNITXML
    base-directory: output